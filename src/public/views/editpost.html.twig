
{% extends "/templates/base.html.twig" %}

{% block title %}
    New Post
{% endblock %}

{% block head %}
    {{ parent() }}
    <style type="text/css"> 
        body {text-align: center;}
    </style>
{% endblock %}

{% block content %}
    <h1>New Post</h1>
    
    <form action="/post" method="POST" >
        <p><input type="text" placeholder="Title" name="title" value="{{post.Title}}"></p>
        <p>
            <select name="category">
            {% for cat in categories %}
                <option value="{{cat.CategoryId}}" {{cat.sel}}>{{cat.Name}}</option>
            {% endfor %}
            </select>
        </p>
        
        <p>
            <select name="author">
                {% for a in authors %}
                    <option value="{{a.AccountId}}" {{a.sel}}>{{a.FirstName}} {{a.LastName}}</option>
                {% endfor %}
            </select>
        </p>
        <p><textarea placeholder="Content" name="content" rows="10" cols="50">{{post.Content}}</textarea></p>
    
        <button type="submit">Save Post!</button>
    </form>
    
    
    
{% endblock %}