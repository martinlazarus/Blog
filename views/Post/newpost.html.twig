{% extends "/templates/base.html.twig" %}

{% block title %}
    New Post
{% endblock %}

{% block head %}
    {{ parent() }}
{% endblock %}

{% block content %}
    <h1>New Post</h1>
    
    <form action="/post" method="POST" >
        <p>Title: <input type="text" placeholder="Title" name="title"></p>
        <p>Category: <select name="category">
            {% for cat in categories %}
                <option value="{{cat.CategoryId}}">{{cat.CategoryName}}</option>
            {% endfor %}
        </select></p>
        
        <p>Author: <select name="author">
            {% for author in authors %}
                <option value="{{author.AuthorId}}">{{author.DisplayName}}</option>
            {% endfor %}
            </select></p>
        <p><textarea placeholder="Content" name="content" rows="10" cols="50"></textarea></p>
        <button type="submit">Save Post!</button>
    </form>
    
{% endblock %}